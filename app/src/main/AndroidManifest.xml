<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bujok.ragstoriches">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ShopStockLevelActivity"
            android:label="@string/title_activity_shop_stock_level"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.bujok.ragstoriches.MainActivity" />
        </activity>
        <!--<receiver
            android:name=".db.DatabaseChangedReceiver"
            android:enabled="true" >
            <intent-filter>
                <action android:name="com.bujok.ragstoriches.DATABASE_CHANGED"></action>
            </intent-filter>
        </receiver>-->

    </application>

</manifest>
